import React from "react";
import styled from "styled-components";

const Searching = () => {
    return <Container>searching</Container>;
};

//styling 💅🏻
const Container = styled.div`
    position: fixed;
    transform: translate(-50%, -50%);
    left: 50%;
    top: 50%;
`;
export default Searching;
